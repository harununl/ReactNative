import { StyleSheet } from "react-native";

export default StyleSheet.create({

    container:{
        justifyContent: 'center',
        flex: 1, 
        padding: 15,
       
    
    },
    header:{
        fontSize: 60,
        color: 'black',
        fontWeight: 'bold',
        textAlign: 'center'
    }

})